ARG ENTANDO_IMAGE_VERSION
FROM entando/entando-wildfly12-base:$ENTANDO_IMAGE_VERSION as dependencies
ARG ENTANDO_VERSION
ARG MAVEN_MIRROR_FOR_DOCKER_BUILDS
USER $USERID_TO_USE
CMD "${ENTANDO_COMMON_PATH}/start-war-in-jetty.sh"
